# בלאק‑ג'ק רב‑משתתפים – תיק פרויקט

---

## דף שער

| שדה | פרטים |
|---|---|
| **לוגו בית הספר** | [לוגו] |
| **שם בית הספר** | [שם בית הספר] |
| **שם העבודה** | בלאק‑ג'ק רב‑משתתפים – משחק קלפים מקוון מאובטח |
| **שם התלמיד** | [שם מלא] |
| **ת.ז. התלמיד** | [מספר ת.ז.] |
| **שם המנחה** | [שם המנחה] |
| **שם החלופה** | הגנת סייבר ומערכות הפעלה |
| **תאריך ההגשה** | [תאריך] |

**חלופת התמחות:** הגנת סייבר ומערכות הפעלה
**מסלול:** הנדסת תוכנה 883589

---

# 1. מבוא

## 1.1 ייזום

### 1.1.1 תיאור ראשוני של המערכת

הפרויקט הוא משחק **בלאק‑ג'ק (21) רב‑משתתפים** על רשת בארכיטקטורת לקוח‑שרת. מספר שחקנים מתחברים ממחשבים שונים לשרת מרכזי, ומשחקים יחד כנגד דילר ממוחשב.

מה שנבנה בסופו של דבר:

* **שרת משחק** – מקבל חיבורים, מנהל את חוקי המשחק, שומר פרופילי שחקנים ומחזיק הכול ב‑SQLite על הדיסק.
* **לקוח גרפי (Tkinter)** – מסך התחברות/הרשמה, שולחן בלאק‑ג'ק עם קלפים, כפתורי Hit/Stand/Double, שדה הימור, סרגל פרופיל וצ'אט בזמן אמת.
* **לוח בקרה לשרת (ServerApp)** – חלון ניהול נפרד: רשימת שחקנים מחוברים, כפתור Kick לכל שחקן, לוג צבעוני ושלב המשחק הנוכחי.
* **סינון צ'אט עם AI (ChatModerator)** – כל הודעת צ'אט עוברת דרך Gemini 2.5 Flash לפני שידורה. אם המפתח לא מוגדר, המשחק עובד בלעדיו בלי בעיה.
* **שכבת הצפנה** – לחיצת יד RSA + Fernet: כל הודעה מוצפנת ומאומתת.
* **שכבת הרשאות** – סיסמאות מגובבות (PBKDF2), כל פעולה מחייבת שהשחקן מחובר ומאומת.

**למה בחרתי בבלאק‑ג'ק:** הוא פשוט מספיק בחוקיו כדי שהמיקוד יישאר בנושאי החלופה – תקשורת, ריבוי לקוחות, שמירת מצב והגנת סייבר – אבל מורכב מספיק כדי לדרוש מכונת מצבים, סנכרון בין שחקנים ולוגיקת קלפים אמיתית.

**אתגרים שידעתי שיחכו לי:**

* לתכנן פרוטוקול תקשורת שמתמודד עם הזרם הבלתי מוגדר של TCP.
* לנהל ריבוי לקוחות בו‑זמנית עם threads בלי race conditions.
* לממש נכון לחיצת יד הצפנה אסימטרית–סימטרית וגיבוב סיסמאות.
* לבנות מכונת מצבים שפשוט לא מאפשרת פעולה מחוץ לתור.

### 1.1.2 הגדרת הלקוח

הפרויקט מכוון לשני קהלים:

* **שחקנים** – כל אחד שרוצה משחק קלפים חברתי ברשת מקומית, בלי תשלום אמיתי.
* **מורים ותלמידים בחלופת סייבר** – להדגמה חיה של הצפנה, threads, sockets ואחסון מאובטח.

### 1.1.3 יעדים ומטרות

1. כמה שחקנים יכולים לשחק ביחד סביב אותו שולחן, בו‑זמנית.
2. כל שחקן יכול להירשם, להתחבר ולראות את הפרופיל שלו (קרדיטים, ניצחונות, הפסדים, יחס W/L).
3. כל התקשורת בין הלקוח לשרת מוצפנת ומאומתת – לא ניתן לקרוא אותה ב‑tcpdump.
4. לקוח זדוני לא יכול להשפיע על שחקנים אחרים ולא לזייף יתרה.
5. ממשק פשוט וברור – גם מישהו שלא יודע כלום יוכל להתחיל לשחק.
6. פרופילים נשמרים בין הפעלות – השרת יכול להיסגר ולהיפתח מחדש בלי לאבד נתונים.

### 1.1.4 בעיות, תועלות וחסכונות

* **הבעיה:** רוב משחקי הקלפים המקוונים הם "קופסה שחורה" סגורה, או דורשים תשתית ענן ותשלום חודשי.
* **תועלות:**
  * משחק חברתי ברשת מקומית, ללא הרשמה לאף שירות חיצוני.
  * סביבת לימוד מצוינת להבנת הצפנה, threads ו‑sockets על TCP גולמי.
  * פרופילים נשמרים בין הפעלות – לא מאבדים את הנתונים כשסוגרים את השרת.
* **שירותים שהמערכת מספקת:** הרשמה/התחברות, הימור, חלוקת קלפים, Hit/Stand/Double, משחק דילר אוטומטי, חישוב תוצאות ותשלום, צ'אט, ורענון מצב שולחן בזמן אמת לכל שחקן.

### 1.1.5 סקירת פתרונות קיימים

| מערכת קיימת | יתרונות | חסרונות מבחינתנו |
|---|---|---|
| Online Casino (888, PokerStars) | עיצוב מקצועי | סגור, דורש תשלום, אין גישה לקוד |
| Blackjack ב‑Python (CLI) | קוד פתוח, פשוט | חד‑שחקן, ללא רשת, ללא אבטחה |
| WebSocket Multiplayer בדפדפן | רב‑משתתפים, נגיש | מסתיר את שכבות ה‑TCP, לא מלמד sockets |

הפרויקט שלי שונה: **רב‑משתתפים אמיתי על TCP גולמי**, עם **לחיצת יד מוצפנת שכתבתי ידנית** ושמירת פרופילים ב‑SQLite מקומי – כך שאפשר לקרוא ולהבין כל שכבה בעצמך.

### 1.1.6 סקירת טכנולוגיית הפרויקט

הכול ב‑**Python 3.11**. מרבית הספריות מגיעות עם Python (socket, threading, sqlite3, struct, secrets, hmac, hashlib, tkinter). שתי ספריות חיצוניות בלבד: **cryptography** (RSA + Fernet) ו‑**google-genai** (Gemini לסינון צ'אט).

**מה לא מתאים לפרויקט הזה:**

* הוא לא בשביל אינטרנט פתוח – אין תעודת CA ואין הגנת DDoS מתוחכמת. מיועד לרשת מקומית.
* Tkinter דורש מערכת הפעלה עם תמיכה גרפית – לא יעבוד על שרת headless ללא X.

### 1.1.7 תיחום הפרויקט

**מה יש בפרויקט:**

* תקשורת TCP sockets ופרוטוקול הודעות מותאם.
* ריבוי לקוחות מקביל עם threads.
* הצפנה היברידית (RSA-2048 + Fernet/AES-128) ולחיצת יד מלאה.
* גיבוב סיסמאות (PBKDF2-HMAC-SHA256 + salt ייחודי לכל משתמש).
* מסד נתונים SQLite לשמירת פרופילים.
* ממשק גרפי ב‑Tkinter לשחקנים ולמנהל השרת.
* מכונת מצבים ל‑5 שלבי הסבב.

**מה אין בפרויקט (ובכוונה):**

* כסף אמיתי או תשלומים.
* שרתים בענן או מבנה מבוזר.
* דפדפן / WebSockets / אפליקציה מובייל.
* גרפיקה מיוחדת מעבר לקלפים שמצוירים ב‑Tkinter.

## 1.2 פירוט תיאור המערכת (אפיון)

### 1.2.1 תיאור מפורט של המערכת

המערכת מורכבת משני יישומים נפרדים:

1. **שרת בלאק‑ג'ק** – בריצה ראשונה יוצר זוג מפתחות RSA ומאזין על פורט 5050. לכל לקוח שמתחבר נפתח thread נפרד.
2. **לקוח בלאק‑ג'ק** – חלון Tkinter שמתחבר לשרת, מבצע לחיצת יד מוצפנת, ואז עובר למסך ההתחברות ומשם לשולחן.

### 1.2.2 מה השחקן יכול לעשות

יש סוג משתמש אחד – **שחקן**. אלה הפעולות שהוא יכול לבצע:

1. להירשם (נפתח פרופיל עם 10,000 קרדיטים).
2. להתחבר לשרת קיים.
3. להצטרף לשולחן ולשחק עם השחקנים האחרים.
4. להמר בתחילת כל סבב.
5. לבחור Hit, Stand, או Double בתורו.
6. לראות את קלפי הדילר ושל שחקנים אחרים בשולחן.
7. לעקוב אחר הפרופיל שלו – קרדיטים, ניצחונות, הפסדים ויחס W/L.
8. לשוחח בצ'אט עם שחקנים אחרים.
9. להתנתק.

### 1.2.3 בדיקות מתוכננות (קופסה שחורה)

| # | מטרת הבדיקה | אופן הביצוע |
|---|---|---|
| 1 | הרשמה תקינה | register("alice","secret123") – לוודא אישור ורשומה חדשה בקובץ |
| 2 | מניעת רישום כפול | לרשום "alice" פעמיים – לקבל שגיאה בפעם השנייה |
| 3 | סיסמה שגויה | להזין סיסמה לא נכונה – לקבל הודעה גנרית |
| 4 | מניעת התחברות כפולה | להתחבר עם "alice" פעמיים – החיבור השני נדחה |
| 5 | משחק מלא לשני שחקנים | הימור, Hit/Stand, דילר, תשלום נכון |
| 6 | הימור מעל יתרה | לבקש הימור גדול מהקרדיטים – שגיאה, היתרה לא משתנה |
| 7 | פעולה מחוץ לתור | ללחוץ Hit כשזה לא התור שלך – נדחה בשרת |
| 8 | האזנה לרשת | tcpdump על הפורט – חבילות לאחר handshake לא קריאות |
| 9 | הודעה ענקית | לשלוח מסגרת >64 KB – החיבור נסגר, השרת ממשיך |
| 10 | שחזור פרופיל אחרי restart | לעצור ולהפעיל שוב – קרדיטים וניצחונות נשמרו |
| 11 | ניתוק באמצע סבב | לסגור לקוח בזמן תורו – התור עובר הלאה אוטומטית |
| 12 | ולידציית שם משתמש | לנסות שם עם תווים מיוחדים – נדחה |

### 1.2.4 לוח זמנים

| # | אבן דרך | משך מתוכנן | משך בפועל |
|---|---|---|---|
| 1 | אפיון, לימוד sockets ו‑threads | שבוע 1-2 | שבוע 1-2 |
| 2 | פרוטוקול ולחיצת יד | שבוע 3-4 | שבוע 3-5 (חריגה בגלל debug של encoding) |
| 3 | מנוע משחק (Card/Deck/Hand/Table) | שבוע 5-6 | שבוע 6-7 |
| 4 | ניהול פרופילים והצפנת סיסמאות | שבוע 7 | שבוע 7-8 |
| 5 | שילוב שרת ולקוח, threads, broadcasts | שבוע 8 | שבוע 8-9 |
| 6 | GUI ב‑Tkinter | שבוע 9-10 | שבוע 9-11 |
| 7 | בדיקות אינטגרציה ותיקון באגים | שבוע 11 | שבוע 11-12 |
| 8 | כתיבת תיק הפרויקט | שבוע 12 | שבוע 12 |

### 1.2.5 ניהול סיכונים

| סיכון | תכנון | מה בוצע |
|---|---|---|
| Race conditions | RLock על מצב המשחק | RLock ב‑Table וב‑ProfileManager; כל פעולה משנה‑מצב תחת with self._lock |
| כשל בלחיצת היד | try/except + סגירת חיבור | החיבור נסגר בנימוס; הלקוח מציג הודעת שגיאה |
| שחיתות נתונים בכתיבה | SQLite WAL mode – כתיבות אטומיות | ממומש ב‑ProfileManager עם PRAGMA journal_mode=WAL |
| לקוח שולח הודעה ענקית | מגבלת 64 KB לפני קריאה | ממומש ב‑protocol.py |
| Tkinter לא thread-safe | queue + root.after(0,...) | ממומש ב‑AppController._on_server_message / _process_messages |

## 1.3 תחום הידע – פירוט יכולות

### 1.3.1 יכולות בצד השרת

#### יכולת: קבלת חיבור חדש וניהולו בתהליכון נפרד

* **מהות:** השרת מאזין על פורט קבוע. לכל לקוח נפתח ClientHandler נפרד.
* **פעולות:** socket.accept, threading.Thread, ניהול רשימת מטפלים, סגירה מסודרת.
* **אובייקטים:** BlackjackServer, ClientHandler.

#### יכולת: לחיצת יד הצפנה (RSA → Fernet)

* **מהות:** השרת שולח מפתח ציבורי, הלקוח מגריל מפתח Fernet, מצפין אותו ב‑RSA ושולח. מכאן כל הודעה מוצפנת סימטרית.
* **פעולות:** טעינת/יצירת זוג RSA, פענוח OAEP, יצירת SecureChannel.
* **אובייקטים:** RSAKeyPair, SecureChannel.

#### יכולת: רישום משתמש חדש

* **מהות:** קליטת שם וסיסמה, ולידציה, גיבוב ושמירה.
* **פעולות:** ולידציה, hash_password (PBKDF2), INSERT לטבלת users ב‑SQLite, commit.
* **אובייקטים:** ProfileManager, UserProfile.

#### יכולת: התחברות משתמש קיים

* **מהות:** השוואת סיסמה כנגד הגיבוב השמור, עמידות ב‑timing attacks, מניעת התחברות כפולה.
* **פעולות:** verify_password עם hmac.compare_digest; השהיה דמה אם המשתמש לא קיים.
* **אובייקטים:** ProfileManager, ClientHandler.

#### יכולת: ניהול שולחן הבלאק‑ג'ק

* **מהות:** מכונת מצבים WAITING → BETTING → PLAYING → DEALER → RESULTS.
* **פעולות:** קבלת שחקן, קבלת הימור, חלוקת קלפים, מעבר תור, משחק דילר, חישוב תוצאות, איפוס.
* **אובייקטים:** Table, Player, Dealer, Deck, Hand, Card, Phase.

#### יכולת: שידור מצב השולחן (broadcast)

* **מהות:** לאחר כל אירוע, השרת בונה תמונת מצב לכל שחקן (מסתיר קלף נעלם של דילר) ושולח לו.
* **פעולות:** סריאליזציה של Table, החלפת קלף נעלם, איטרציה על ClientHandler-ים.
* **אובייקטים:** BlackjackServer, Table.snapshot_for, ClientHandler.send.

#### יכולת: תשלום וזיכוי לפי תוצאת הסבב

* **מהות:** השוואת ידיים, תשלום (1:1, 3:2 לבלאק‑ג'ק, החזר בפוש), עדכון פרופיל.
* **פעולות:** Table._resolve_locked, ProfileManager.adjust_credits, record_result.
* **אובייקטים:** Table, ProfileManager.

#### יכולת: סינון הודעות צ'אט (Gemini AI)

* **מהות:** לפני שידור הודעת צ'אט, נשלחת ל‑Gemini 2.5 Flash. אם נחסמה – רק השולח מקבל שגיאה. אם ה‑API לא זמין – ההודעה עוברת.
* **פעולות:** בדיקת GEMINI_API_KEY, קריאת google.genai, פרשנות ALLOW/BLOCK.
* **אובייקטים:** ChatModerator, ClientHandler.

#### יכולת: לוח בקרה גרפי לשרת

* **מהות:** חלון Tkinter בזמן אמת: IP/פורט, רשימת שחקנים מחוברים, Kick, לוג צבעוני, שלב המשחק, ועצירת השרת.
* **פעולות:** Tk window, root.after() כל 400ms, גישה ל‑_online תחת lock.
* **אובייקטים:** ServerApp, PlayerRow, BlackjackServer.

### 1.3.2 יכולות בצד הלקוח

#### יכולת: התחברות לשרת ולחיצת יד

* **מהות:** פתיחת socket, פענוח server_hello, יצירת מפתח Fernet, הצפנה ב‑RSA ושליחה.
* **פעולות:** socket.connect, SecureChannel.generate, encrypt_session_key.
* **אובייקטים:** ServerConnection, SecureChannel.

#### יכולת: מסך התחברות/הרשמה

* **מהות:** שדות שם וסיסמה, שני כפתורים (Login ו‑Register), תשובת השרת מוצגת מתחת.
* **פעולות:** ttk.Frame, שליחת בקשה, טיפול בשגיאה.
* **אובייקטים:** ConnectFrame, LoginFrame, AppController.

#### יכולת: מסך השולחן

* **מהות:** אזור דילר, אזור שחקנים עם הדגשה ל"(you)", פרופיל אישי בסרגל עליון, שדה הימור, כפתורי פעולה.
* **פעולות:** ציור קלף (_card_label), איפשור/נטרול כפתורים לפי שלב (_refresh_buttons).
* **אובייקטים:** TableFrame.

#### יכולת: רענון אסינכרוני של מצב השולחן

* **מהות:** הודעות מהרשת מגיעות ב‑thread רקע. נדחפות לתור, ה‑GUI מנקה אותן ב‑root.after(0,...) על ה‑main thread.
* **פעולות:** queue.Queue, _on_server_message, _process_messages.
* **אובייקטים:** AppController.

#### יכולת: שליחת פעולה (Hit/Stand/Double/Bet/Chat)

* **מהות:** לחיצה מתורגמת להודעה מוצפנת שנשלחת לשרת.
* **פעולות:** ולידציה לפני שליחה, ServerConnection.send, טיפול בכשל רשת.
* **אובייקטים:** TableFrame, AppController, ServerConnection.

---

# 2. מבנה / ארכיטקטורה של הפרויקט

## 2.1 ארכיטקטורה כללית

הפרויקט בנוי על **לקוח‑שרת קלאסי עם שרת מרכזי אחד**. כל הקלפים, הקופה, הכללים והחשבונות מנוהלים בשרת בלבד. הלקוח מציג את המצב ושולח פעולות – הוא **אף פעם לא מקור האמת**.

### 2.1.1 רכיבי חומרה

* **מחשב שרת:** מריץ python -m blackjack.run_server. צריך Python 3.11, חיבור רשת, מקום לשני קבצים (profiles.db, server_rsa.pem).
* **מחשבי לקוח:** כל שחקן מריץ python -m blackjack.run_client באותה רשת.
* **רשת:** TCP/IPv4 על פורט 5050.

### 2.1.2 שרטוט ארכיטקטורה

```
                +--------------------------------------+
                |              Server PC               |
                |   +------------------------------+   |
                |   |   BlackjackServer (main)     |   |
                |   |   - Listening socket :5050   |   |
                |   +------+---------+-------------+   |
                |          |         |                  |
                |    +-----v--+ +----v---+   ...        |
                |    |Handler | |Handler |  thread      |
                |    | (Bob)  | |(Alice) |   per        |
                |    +---+----+ +---+----+  client      |
                |        |         |                    |
                |        +----+----+                    |
                |             |                         |
                |    +--------v---------+               |
                |    |   Table (lock)   |  game state   |
                |    |   ProfileManager |  + persistence|
                |    +--------+---------+               |
                |             |                         |
                |    +--------v---------+               |
                |    | data/            |  filesystem   |
                |    |  profiles.db     |               |
                |    |  server_rsa.pem  |               |
                |    +------------------+               |
                +-----^------------^-------------------+
                      |            |        TCP 5050
                      |            |   (מוצפן לאחר handshake)
            +---------+---+    +---+---------+
            |  Client PC  |    |  Client PC  |
            |  (Bob)      |    |  (Alice)    |
            |  Tkinter UI |    |  Tkinter UI |
            +-------------+    +-------------+
```

## 2.2 טכנולוגיות בשימוש

| תחום | טכנולוגיה | סיבה |
|---|---|---|
| שפת תכנות | Python 3.11 | קריאה, ספריית סטנדרט עשירה |
| תקשורת | TCP גולמי, פורט 5050 | אמין, מאפשר פרוטוקול מותאם |
| הצפנה אסימטרית | RSA-2048 + OAEP | תקן מקובל להעברת מפתח |
| הצפנה סימטרית | Fernet (AES-128-CBC + HMAC-SHA256) | סודיות + אימות שלמות |
| גיבוב סיסמאות | PBKDF2-HMAC-SHA256, 200,000 איטרציות | קשה ל‑brute-force |
| ממשק משתמש | Tkinter | בנוי ב‑Python, ללא תלות |
| אחסון | SQLite (sqlite3) – WAL mode | עמיד בפני קריסה, תומך בשאילתות SQL, חלק מהספרייה הסטנדרטית |
| ריבוי לקוחות | threading.Thread + RLock | פשוט ומספיק יעיל |
| סינון צ'אט | Gemini 2.5 Flash (google-genai) | מבין הקשר ושפות |

## 2.3 זרימת מידע

### 2.3.1 חיבור שחקן

```
[Client]                                    [Server]
   |  TCP connect :5050                        |
   |------------------------------------------>|
   |  server_hello {public_key_pem}            |
   |<------------------------------------------|
   |  key_exchange {encrypted_session_key}     |
   |------------------------------------------>|
   |  ready (encrypted)                        |
   |<------------------------------------------|
   |  login {username, password} (encrypted)   |
   |------------------------------------------>|
   |                                  [authenticate]
   |  auth_result {ok, profile} (encrypted)    |
   |<------------------------------------------|
   |  game_state {snapshot} (encrypted)        |
   |<------------------------------------------|
```

### 2.3.2 זרימת סבב

```
BETTING:
  client --place_bet 200--> server --> Table, ProfileManager
  server --> broadcast game_state --> all clients

PLAYING:
  Table.deal() -> 2 cards each, dealer hidden card
  server -> snapshot for each client
  Loop: client --action {hit|stand|double}--> server --> Table
  server -> broadcast

DEALER:
  server reveals dealer hole card
  while dealer.should_hit(): dealer.add(deck.draw())
  server -> broadcast

RESULTS:
  compute outcome, payout, record W/L
  server -> round_result + updated profile
  Timer (6s) -> next round
```

## 2.4 אלגוריתמים מרכזיים

### 2.4.1 חישוב ערך יד עם אסים גמישים

**הבעיה:** אס שווה 1 או 11 – צריך לבחור את הצירוף שמקסם את הערך בלי לחצות 21.

| פתרון | סיבוכיות | הערות |
|---|---|---|
| בדיקה רקורסיבית של כל הצירופים | O(2^k) | יקר ומסורבל |
| ספירת אסים כ‑1, "שדרוג" כל עוד לא חוצים 21 | O(n) | פשוט, בדיוק מה שקזינואים עושים |

**הבחירה:** הפתרון השני. ראה common/hand.py → Hand.value.

### 2.4.2 ערבוב מאובטח

**הבעיה:** סדר הקלפים חייב להיות בלתי ניתן לחיזוי.

| פתרון | הבעיה איתו |
|---|---|
| random.shuffle | Mersenne Twister – ניתן לחיזוי אחרי 624 ערכים |
| Fisher-Yates + secrets.randbelow | מקור אקראי קריפטוגרפי (/dev/urandom), לא ניתן לחיזוי |

**הבחירה:** Fisher-Yates עם secrets.randbelow. ראה common/deck.py → Deck.shuffle.

### 2.4.3 גיבוב סיסמאות PBKDF2

**הבעיה:** צריך לאחסן סיסמאות כך שאם מישהו יגנוב את הקובץ, הוא לא יוכל לשחזר אותן.

| פתרון | הבעיה איתו |
|---|---|
| MD5 / SHA-1 פשוט | מהיר מדי – מיליוני ניחושים בשנייה, פגיע ל‑rainbow tables |
| PBKDF2 + salt | 200,000 איטרציות – כל ניחוש בודד עולה המון זמן |

**הבחירה:** PBKDF2-HMAC-SHA256, 200,000 איטרציות, salt אקראי ייחודי לכל משתמש. חלק מ‑hashlib הסטנדרטית. ראה common/crypto.py.

### 2.4.4 מסגור הודעות מעל TCP

**הבעיה:** TCP מספק זרם בתים רציף, לא הודעות מסודרות. צריך לדעת איפה הודעה אחת נגמרת והבאה מתחילה.

| פתרון | הערה |
|---|---|
| delimiter (\\n) | עלול להופיע בתוך תוכן מוצפן |
| length-prefix 4 בתים | בינארי בטוח, פשוט, מאפשר הגבלת גודל לפני קריאה |

**הבחירה:** length-prefix big-endian 4 בתים + מגבלת 64 KB לפני כל קריאה. ראה common/protocol.py.

### 2.4.5 סינון צ'אט בזמן אמת עם Gemini AI

**הבעיה:** שחקנים יכולים לשלוח הודעות פוגעניות. רשימת מילים שחורות לא מספיקה – קל מדי לעקוף אותה עם ניסוחים יצירתיים.

| פתרון | החיסרון |
|---|---|
| Blocklist מילים | קל לעקוף, לא מבין הקשר |
| Perspective API | תלות חיצונית נוספת |
| Gemini 2.5 Flash | מבין הקשר, עברית ואנגלית, מהיר, ה‑API מוכן |

**הבחירה:** Gemini 2.5 Flash עם ה‑system prompt הבא:

> "You are a strict content moderation assistant for an online multiplayer card game. Your only task is to decide whether a player chat message contains vulgar, hateful, sexually explicit, racist, threatening, or otherwise offensive content. Reply with exactly one word: ALLOW or BLOCK."

אם Gemini מחזיר BLOCK – השרת שולח שגיאה **רק לשולח**. שאר השחקנים לא רואים כלום. אם ה‑API לא זמין – ההודעה עוברת כברירת מחדל (pass-through mode).

קובץ: server/chat_moderator.py → ChatModerator → check(message).

```python
allowed, reason = _moderator.check(text)
if not allowed:
    self.send("error", {"message": reason})
else:
    self._server.on_chat(self, text)
```

## 2.5 סביבת פיתוח

* Python 3.11, VS Code עם תוסף Python.
* חבילות: pip install cryptography google-genai.
* git לניהול גרסאות.
* בדיקות ידניות: שרת + שני לקוחות בו‑זמנית; tcpdump לוידוא שהתעבורה מוצפנת; ls -l לוידוא הרשאות PEM.

## 2.6 פרוטוקול תקשורת

### 2.6.1 שכבות הפרוטוקול

1. **שכבת מסגור:** 4 בתים big-endian (אורך) + n בתים גוף. מגבלה: 0 < n ≤ 65,536 – כל ניסיון לשלוח יותר סוגר את החיבור.
2. **שכבת הצפנה:** לפני סיום handshake – JSON גלוי. לאחר מכן – פלט Fernet (מוצפן + HMAC אוטומטי).
3. **שכבת הודעה:** JSON עם המבנה `{"type": "...", "data": {...}}`. סוג ההודעה חייב להיות מתוך רשימה סגורה – שאר הסוגים נדחים.

### 2.6.2 פירוט ההודעות

| שם הודעה | מ→אל | שדות |
|---|---|---|
| server_hello | שרת → לקוח | public_key_pem: str |
| key_exchange | לקוח → שרת | encrypted_session_key: str (base64) |
| ready | שרת → לקוח | {} |
| register | לקוח → שרת | username: str, password: str |
| login | לקוח → שרת | username: str, password: str |
| auth_result | שרת → לקוח | ok: bool, message: str, profile?: dict |
| place_bet | לקוח → שרת | amount: int |
| action | לקוח → שרת | action: "hit" / "stand" / "double" |
| game_state | שרת → כולם | phase, dealer, players, current_username, deck_remaining |
| round_result | שרת → לקוח | outcome: win/loss/push, payout: int, profile: dict |
| chat | לקוח → שרת → כולם | message: str (עד 200 תווים), עובר סינון Gemini |
| error | שרת → לקוח | message: str |
| logout | לקוח → שרת | {} |

## 2.7 מסכי המערכת

### 2.7.1 מסך חיבור לשרת (ConnectFrame)

**תפקיד:** מסך הפתיחה – מאפשר להזין כתובת שרת לפני כניסה לחשבון.

**מה מוצג:**
* שדה "Server host" (ברירת מחדל: 127.0.0.1).
* שדה "Port" (ברירת מחדל: 5050).
* כפתור "Join Game".
* תווית מצב לשגיאות חיבור.

**מה ניתן לבצע:** להזין כתובת ופורט ולחיצה על Join Game מעבירה למסך ההתחברות.

*תמונת מסך: [יש לצלם מסך בעת הרצת המערכת]*

---

### 2.7.2 מסך התחברות / הרשמה (LoginFrame)

**תפקיד:** מסך הזדהות – התחברות לחשבון קיים או פתיחת חשבון חדש.

**מה מוצג:**
* שדה Username, שדה Password (תווים מוסתרים ●).
* כפתורי Login ו‑Register.
* תווית מצב (הצלחה / שגיאה).

**מה ניתן לבצע:** להתחבר עם פרטים קיימים, או להירשם כמשתמש חדש.

*תמונת מסך: [יש לצלם מסך בעת הרצת המערכת]*

---

### 2.7.3 מסך השולחן (TableFrame)

**תפקיד:** המסך הראשי של המשחק – מציג את שולחן הקלפים ומאפשר כל פעולות המשחק.

**מה מוצג:**
* סרגל פרופיל עליון: שם שחקן, קרדיטים, ניצחונות, הפסדים, W/L, כפתור Logout.
* אזור דילר: קלפי הדילר, ערך, ציון BLACKJACK/BUST.
* אזור שחקנים: שורה לכל שחקן – שם, הימור, קלפים, ערך (שחקן עצמו מסומן).
* שדה הימור + כפתור "Place Bet".
* כפתורי Hit, Stand, Double (מתאפשרים/ננעלים לפי שלב ותור).
* לוח צ'אט: הודעות נכנסות + שדה שליחה.

**מה ניתן לבצע:** הימור, Hit/Stand/Double, שליחת צ'אט, התנתקות.

*תמונת מסך: [יש לצלם מסך בעת הרצת המערכת]*

---

### 2.7.4 תוצאת סבב (בתוך TableFrame)

**תפקיד:** הצגת תוצאת הסבב בשדה השלב, למשך ~6 שניות.

**מה מוצג:**
* WIN → הודעה בצבע זהב + מספר קרדיטים שזוכו.
* LOSS → הודעה בצבע אדום.
* PUSH → הודעה בצבע אפור + קרדיטים שהוחזרו.

*תמונת מסך: [יש לצלם מסך בעת הרצת המערכת]*

---

### 2.7.5 לוח בקרה לשרת (ServerApp)

**תפקיד:** חלון ניהול נפרד שרץ על מחשב השרת – מציג מצב בזמן אמת.

**מה מוצג:**
* כותרת: שם האפליקציה + Host/IP/Port.
* שמאל: רשימת שחקנים מחוברים + קרדיטים + אחוז ניצחונות + כפתור Kick לכל אחד.
* ימין: לוג צבעוני לפי חומרה (DEBUG=אפור, INFO=לבן, WARNING=זהב, ERROR=אדום).
* שורת סטטוס תחתונה: שלב משחק + קלפים שנותרו + כפתור Stop Server.

**מה ניתן לבצע:** לצפות בלוג בזמן אמת, להרחיק שחקן (Kick), לעצור את השרת.

*תמונת מסך: [יש לצלם מסך בעת הרצת המערכת]*

---

### 2.7.6 תרשים מסכים

```
  [ConnectFrame]
       |
       | Join Game (כתובת + פורט)
       v
  [LoginFrame] <-----------+
       |                   |
       | Register / Login  | disconnect / network error
       |                   |
       v                   |
  [TableFrame] ------------+
       |
       | logout
       v
  [LoginFrame]

  [ServerApp]  ← רץ בנפרד על מחשב השרת
```

## 2.8 מבני נתונים

### 2.8.1 טבלת users – מסד הנתונים profiles.db

מסד הנתונים הוא קובץ SQLite יחיד. הטבלה נוצרת אוטומטית בריצה ראשונה.

```sql
CREATE TABLE IF NOT EXISTS users (
    username      TEXT    PRIMARY KEY,
    password_hash TEXT    NOT NULL,
    credits       INTEGER NOT NULL DEFAULT 10000,
    wins          INTEGER NOT NULL DEFAULT 0,
    losses        INTEGER NOT NULL DEFAULT 0,
    pushes        INTEGER NOT NULL DEFAULT 0
);
```

| שם עמודה | טיפוס | אורך / טווח | דוגמה |
|---|---|---|---|
| username | TEXT (PRIMARY KEY) | 3-16 תווים, A-Za-z0-9_ | "alice" |
| password_hash | TEXT | פורמט salt_b64$hash_b64 | "k4Ja…$g2Cv…" |
| credits | INTEGER | >= 0 | 9750 |
| wins | INTEGER | >= 0 | 4 |
| losses | INTEGER | >= 0 | 2 |
| pushes | INTEGER | >= 0 | 1 |

SQLite פועל במצב WAL (Write-Ahead Log) – כתיבות אטומיות מובנות, עמיד בפני קריסה.

### 2.8.2 קובץ server_rsa.pem

מפתח RSA פרטי בפורמט PEM. הרשאות 0o600 (קריאה/כתיבה לבעל בלבד). נוצר אוטומטית בריצה ראשונה.

### 2.8.3 מבני נתונים בזיכרון

* BlackjackServer._clients : List[ClientHandler]
* BlackjackServer._online : Dict[str, ClientHandler]
* Table._players : List[Player]
* Deck._cards : List[Card]
* AppController._msg_queue : queue.Queue

## 2.9 סקירת חולשות ואיומים

### 2.9.1 שכבת האפליקציה

| איום | רלוונטי? | מה נעשה |
|---|---|---|
| SQL Injection | כן | כל שאילתה משתמשת בפרמטרים (?) – אין שרשור מחרוזות עם קלט משתמש |
| אימות (login) | כן | PBKDF2 + salt; hmac.compare_digest (constant-time); הודעת שגיאה גנרית; מניעת login כפול |
| MITM | כן | RSA-2048 + OAEP + Fernet; שינוי בית אחד בחבילה זורק חריגה מיידית |
| DoS | חלקי | מגבלת 64 KB לפני קריאה; thread לכל לקוח; socket timeout |
| Fuzzing | כן | parser קפדני עם רשימה סגורה; ProtocolError סוגר חיבור |
| גישה לנתוני שחקן אחר | כן | _must_be_logged_in; snapshot_for מסתיר את הקלף הנעלם של הדילר |
| אקראיות חלשה | כן | secrets.randbelow (/dev/urandom), לא random רגיל |
| חשיפת מפתח Gemini | כן | רק כמשתנה סביבה; לא בקוד, לא בלוג, לא ב‑git |

### 2.9.2 שכבת התעבורה

* TCP – לחיצת היד המשולשת מנוהלת על ידי מערכת ההפעלה; אין מה להוסיף כאן.
* הצפנה: hybrid handshake (RSA + Fernet) – סודיות + שלמות, בלי להזדקק ל‑TLS מלא.
* _read_exactly מבטיח שלא נקרא חצי הודעה, גם כשה‑socket מחזיר פחות בתים ממה שביקשנו.

---

# 3. מימוש הפרויקט

## 3.1 חלק א' – תיאור מחלקות וספריות

### 3.1.1 ספריות חיצוניות

| ספרייה | שימוש |
|---|---|
| cryptography.hazmat.primitives.asymmetric.rsa | RSA-2048 + OAEP |
| cryptography.hazmat.primitives.serialization | פורמט PEM |
| cryptography.fernet.Fernet | הצפנה סימטרית מאומתת |
| google.genai | קריאות ל‑Gemini 2.5 Flash לסינון צ'אט |

### 3.1.2 מחלקות שפותחו בפרויקט

#### Card (common/card.py)
* **תפקיד:** קלף בודד, immutable – לא ניתן לשנות אחרי יצירה.
* **תכונות:** `rank: str` ('A','2'..'K'), `suit: str` ('S','H','D','C').
* **פעולות:**

| פעולה | טענת כניסה (קלט) | טענת יציאה (פלט) |
|---|---|---|
| `__init__(rank, suit)` | rank: str, suit: str | – ; מאמת ערכים, זורק ValueError אם לא תקין |
| `base_value` (property) | – | int: 1 לאסים, 10 לפנים (J/Q/K), ערך מספרי אחרת |
| `is_ace` (property) | – | bool: True אם rank == 'A' |
| `to_dict()` | – | dict: `{"rank": str, "suit": str}` |
| `from_dict(d)` (classmethod) | d: dict | Card חדש מה‑dict |

---

#### Deck (common/deck.py)
* **תפקיד:** "shoe" של N חבילות מעורבבות – מקור הקלפים לשולחן.
* **תכונות:** `_cards: list[Card]`, `_num_decks: int`.
* **פעולות:**

| פעולה | טענת כניסה (קלט) | טענת יציאה (פלט) |
|---|---|---|
| `__init__(num_decks)` | num_decks: int (ברירת מחדל 4) | – ; יוצר ומערבב את החפיסה |
| `shuffle()` | – | – ; מאפס ומערבב (Fisher-Yates + secrets.randbelow) |
| `draw()` | – | Card: הקלף העליון; זורק ValueError אם ריק |
| `__len__()` | – | int: כמות קלפים שנותרו |

---

#### Hand (common/hand.py)
* **תפקיד:** יד של שחקן או דילר עם חישוב ערך חכם (אסים גמישים).
* **תכונות:** `_cards: list[Card]`.
* **פעולות:**

| פעולה | טענת כניסה (קלט) | טענת יציאה (פלט) |
|---|---|---|
| `__init__(cards)` | cards: list[Card] (אופציונלי) | – |
| `add(card)` | card: Card | – ; מוסיף קלף ליד |
| `clear()` | – | – ; מרוקן את היד |
| `value` (property) | – | int: ערך מיטבי שלא עולה על 21 |
| `is_soft` (property) | – | bool: True אם יש אס שנחשב כ‑11 |
| `is_blackjack` (property) | – | bool: True אם בדיוק 2 קלפים שוים 21 |
| `is_bust` (property) | – | bool: True אם value > 21 |
| `to_list()` | – | list[dict]: כל קלף כ‑dict |

---

#### SecureChannel (common/crypto.py)
* **תפקיד:** עטיפה ל‑Fernet – ערוץ סימטרי מוצפן + מאומת לכל חיבור TCP.
* **תכונות:** `key: bytes` (מפתח Fernet).
* **פעולות:**

| פעולה | טענת כניסה (קלט) | טענת יציאה (פלט) |
|---|---|---|
| `generate()` (classmethod) | – | SecureChannel עם מפתח Fernet אקראי |
| `__init__(key)` | key: bytes | – |
| `encrypt(plaintext)` | plaintext: bytes | bytes: token מוצפן + HMAC |
| `decrypt(token)` | token: bytes | bytes: plaintext; זורק InvalidToken אם פגום |

---

#### RSAKeyPair (common/crypto.py)
* **תפקיד:** זוג מפתחות RSA-2048 של השרת – משמשים להעברת מפתח ה‑session.
* **פעולות:**

| פעולה | טענת כניסה (קלט) | טענת יציאה (פלט) |
|---|---|---|
| `generate()` (classmethod) | – | RSAKeyPair חדש עם מפתחות RSA-2048 |
| `load_or_create(path)` (classmethod) | path: str | RSAKeyPair: טוען מקובץ PEM קיים, או יוצר ושומר |
| `public_pem()` | – | str: מפתח ציבורי בפורמט PEM |
| `decrypt_session_key(b64)` | b64: str (base64) | bytes: מפתח Fernet מפוענח ב‑OAEP |

---

#### UserProfile (server/profile_manager.py)
* **תפקיד:** ייצוג בזיכרון של חשבון שחקן אחד.
* **תכונות:** `username: str`, `password_hash: str`, `credits: int`, `wins: int`, `losses: int`, `pushes: int`.
* **פעולות:**

| פעולה | טענת כניסה (קלט) | טענת יציאה (פלט) |
|---|---|---|
| `games_played` (property) | – | int: wins + losses + pushes |
| `win_loss_ratio` (property) | – | float: wins/losses, או 0 אם אין הפסדים |
| `to_dict()` | – | dict: כל שדות הפרופיל ללא password_hash |

---

#### ProfileManager (server/profile_manager.py)
* **תפקיד:** ממשק ל‑SQLite (profiles.db) – thread-safe לרישום, אימות, קרדיטים ותוצאות.
* **תכונות:** `_conn: sqlite3.Connection`, `_lock: RLock`.
* **פעולות:**

| פעולה | טענת כניסה (קלט) | טענת יציאה (פלט) |
|---|---|---|
| `register(username, password)` | username: str, password: str | UserProfile; זורק ValueError אם שם תפוס או לא תקין |
| `authenticate(username, password)` | username: str, password: str | UserProfile אם תקין, None אחרת |
| `adjust_credits(username, delta)` | username: str, delta: int | int: יתרה חדשה; זורק ValueError אם תוצאה שלילית |
| `record_result(username, outcome)` | username: str, outcome: str ('win'/'loss'/'push') | UserProfile: פרופיל מעודכן |
| `get(username)` | username: str | UserProfile |
| `validate_username(u)` (classmethod) | u: str | –; זורק ValueError אם לא תקין (אורך/תווים) |
| `validate_password(p)` (classmethod) | p: str | –; זורק ValueError אם קצרה מ‑6 תווים |

---

#### Phase (server/game.py)
* **תפקיד:** enum של חמשת שלבי הסבב.
* **ערכים:** `WAITING`, `BETTING`, `PLAYING`, `DEALER`, `RESULTS`.

---

#### RoundResult (server/game.py)
* **תפקיד:** dataclass עם תוצאת סבב לשחקן אחד.
* **שדות:** `username: str`, `outcome: str` ('win'/'loss'/'push'), `payout: int` (קרדיטים לזכות).

---

#### Table (server/game.py)
* **תפקיד:** מכונת מצבים של שולחן הבלאק‑ג'ק – מנהלת שחקנים, הימורים ותורות.
* **תכונות:** `_players: list[Player]`, `_dealer: Dealer`, `_deck: Deck`, `_phase: Phase`, `lock: RLock`.
* **פעולות:**

| פעולה | טענת כניסה (קלט) | טענת יציאה (פלט) |
|---|---|---|
| `add_player(username)` | username: str | –; מוסיף שחקן לשולחן |
| `remove_player(username)` | username: str | –; מסיר שחקן (אפילו באמצע סבב) |
| `place_bet(username, amount, balance)` | username: str, amount: int, balance: int | –; זורק ValueError אם לא בשלב BETTING |
| `handle_action(username, action, can_afford_double)` | username: str, action: str, can_afford_double: bool | –; מבצע hit/stand/double; זורק ValueError אם לא התור |
| `get_player(username)` | username: str | Player \| None |
| `reset_for_next_round()` | – | –; מאפס ידיים והימורים, חוזר לשלב BETTING |
| `snapshot_for(username)` | username: str | dict: תמונת מצב מלאה של השולחן (קלף דילר נסתר בשלב PLAYING) |

---

#### ClientHandler (server/client_handler.py) – thread
* **תפקיד:** thread אחד לכל לקוח מחובר – מחזיק socket וסשן מוצפן.
* **תכונות:** `profile: UserProfile | None`, `_channel: SecureChannel | None`.
* **פעולות:**

| פעולה | טענת כניסה (קלט) | טענת יציאה (פלט) |
|---|---|---|
| `is_logged_in` (property) | – | bool: True אם profile לא None |
| `username` (property) | – | str \| None: שם המשתמש המחובר |
| `send(msg_type, data)` | msg_type: str, data: dict | –; שולח הודעה מוצפנת; thread-safe |
| `shutdown()` | – | –; סוגר socket ועוצר את ה‑thread |
| `run()` | – | –; handshake ואז לולאת הודעות עד סגירה |

---

#### BlackjackServer (server/server.py)
* **תפקיד:** שרת ראשי – accept loop, ניהול שחקנים, תיאום משחק ו‑broadcasts.
* **פעולות:**

| פעולה | טענת כניסה (קלט) | טענת יציאה (פלט) |
|---|---|---|
| `serve_forever()` | – | –; חוסם עד שמפעילים stop() |
| `stop()` | – | –; סוגר socket, מנתק כל לקוח |
| `is_online(username)` | username: str | bool: האם שחקן מחובר כרגע |
| `on_player_joined(handler)` | handler: ClientHandler | –; רושם שחקן ומתחיל broadcast |
| `on_player_left(handler)` | handler: ClientHandler | –; מסיר שחקן ומעדכן שולחן |
| `on_bet(handler, amount)` | handler: ClientHandler, amount: int | –; מחייב קרדיטים ומעדכן שולחן |
| `on_action(handler, action)` | handler: ClientHandler, action: str | –; מעביר פעולה לשולחן |
| `on_chat(handler, message)` | handler: ClientHandler, message: str | –; משדר לכל המחוברים |

---

#### ServerConnection (client/network.py)
* **תפקיד:** חיבור מוצפן מהלקוח לשרת, כולל thread האזנה ברקע.
* **פעולות:**

| פעולה | טענת כניסה (קלט) | טענת יציאה (פלט) |
|---|---|---|
| `connect()` | – | –; פותח TCP socket ומבצע handshake |
| `send(msg_type, data)` | msg_type: str, data: dict | –; שולח הודעה מוצפנת; thread-safe |
| `close()` | – | –; סוגר socket ועוצר thread האזנה |

---

#### AppController (client/gui.py)
* **תפקיד:** בקר ראשי – מחבר בין הרשת ל‑GUI, מנהל מעברי מסך ותור הודעות.
* **פעולות:**

| פעולה | טענת כניסה (קלט) | טענת יציאה (פלט) |
|---|---|---|
| `run()` | – | –; מפעיל את לולאת ה‑Tkinter |
| `send(msg_type, data)` | msg_type: str, data: dict | –; שולח הודעה לשרת |
| `connect(host, port)` | host: str, port: int | –; שומר כתובת ועובר ל‑LoginFrame |
| `login(username, password)` | username: str, password: str | –; מתחבר ושולח login |
| `register(username, password)` | username: str, password: str | –; מתחבר ושולח register |
| `logout()` | – | –; מנתק ועובר ל‑LoginFrame |

---

#### ChatModerator (server/chat_moderator.py)
* **תפקיד:** סינון צ'אט עם Gemini – מחליט אם הודעה מותרת לפני שידורה.
* **פעולות:**

| פעולה | טענת כניסה (קלט) | טענת יציאה (פלט) |
|---|---|---|
| `check(message)` | message: str | tuple[bool, str]: (מותר?, סיבה/הודעת שגיאה) |

---

#### ServerApp (server/gui.py)
* **תפקיד:** לוח בקרה גרפי לשרת – מציג שחקנים, לוג ושלב משחק בזמן אמת.
* **פעולות:**

| פעולה | טענת כניסה (קלט) | טענת יציאה (פלט) |
|---|---|---|
| `run()` | – | –; מפעיל שרת ב‑thread ואת לולאת ה‑Tkinter |
| `_poll()` | – | –; מתעדכן כל 400ms (שחקנים, לוג, שלב) |
| `_kick(username)` | username: str | –; מנתק שחקן לפי שם |
| `_on_stop_click()` | – | –; מבקש אישור ועוצר את השרת |

## 3.2 קטעי קוד מרכזיים – כאן אפשר לראות את העקרונות בפעולה

### 3.2.1 חישוב ערך יד עם אסים

```python
# blackjack/common/hand.py
@property
def value(self) -> int:
    total = sum(c.base_value for c in self._cards)  # all aces start as 1
    aces = sum(1 for c in self._cards if c.is_ace)
    while aces > 0 and total + 10 <= 21:
        total += 10           # promote one ace from 1 to 11
        aces -= 1
    return total
```

### 3.2.2 ערבוב מאובטח של החבילה

```python
# blackjack/common/deck.py
def shuffle(self) -> None:
    self._cards = [
        Card(rank, suit)
        for _ in range(self._num_decks)
        for suit in Card.SUITS
        for rank in Card.RANKS
    ]
    # Fisher-Yates with secrets.randbelow — קריפטוגרפי, לא ניתן לחיזוי
    for i in range(len(self._cards) - 1, 0, -1):
        j = secrets.randbelow(i + 1)
        self._cards[i], self._cards[j] = self._cards[j], self._cards[i]
```

### 3.2.3 לחיצת יד הצפנה (צד שרת)

```python
# blackjack/server/client_handler.py
def _handshake(self) -> None:
    send_plain(self._sock, "server_hello",
               {"public_key_pem": self._rsa.public_pem()})
    msg = recv_plain(self._sock)
    if msg["type"] != "key_exchange":
        raise ProtocolError("expected key_exchange")
    session_key = self._rsa.decrypt_session_key(
        msg["data"]["encrypted_session_key"])
    self._channel = SecureChannel(session_key)
    send_encrypted(self._sock, self._channel, "ready", {})
```

### 3.2.4 גיבוב והשוואת סיסמאות

```python
# blackjack/common/crypto.py
ITERATIONS = 200_000

def hash_password(password: str) -> str:
    salt = secrets.token_bytes(16)
    digest = hashlib.pbkdf2_hmac(
        "sha256", password.encode("utf-8"), salt, ITERATIONS)
    return f"{base64.b64encode(salt).decode()}$" \
           f"{base64.b64encode(digest).decode()}"

def verify_password(password: str, stored: str) -> bool:
    try:
        salt_b64, hash_b64 = stored.split("$", 1)
        salt = base64.b64decode(salt_b64)
        expected = base64.b64decode(hash_b64)
    except (ValueError, binascii.Error):
        return False
    actual = hashlib.pbkdf2_hmac(
        "sha256", password.encode("utf-8"), salt, ITERATIONS)
    return hmac.compare_digest(actual, expected)  # constant-time
```

### 3.2.5 מסגור הודעה עם הגנה על גודל

```python
# blackjack/common/protocol.py
MAX_MESSAGE_BYTES = 64 * 1024
_LENGTH_PREFIX = struct.Struct(">I")

def send_frame(sock, payload):
    if len(payload) > MAX_MESSAGE_BYTES:
        raise ProtocolError("payload too large")
    sock.sendall(_LENGTH_PREFIX.pack(len(payload)) + payload)

def recv_frame(sock):
    header = _recv_exact(sock, _LENGTH_PREFIX.size)
    (length,) = _LENGTH_PREFIX.unpack(header)
    if length == 0 or length > MAX_MESSAGE_BYTES:
        raise ProtocolError(f"invalid frame length: {length}")
    return _recv_exact(sock, length)
```

### 3.2.6 צילום מצב בטוח לכל שחקן

```python
# blackjack/server/game.py
def snapshot_for(self, username: str) -> dict:
    with self._lock:
        dealer_cards = self._dealer.hand.to_list()
        if self._phase is Phase.PLAYING and len(dealer_cards) >= 2:
            dealer_cards = [dealer_cards[0], {"rank": "?", "suit": "?"}]
            value_hidden = True
        else:
            value_hidden = False
        return {
            "phase": self._phase.value,
            "dealer": {"cards": dealer_cards,
                       "value_hidden": value_hidden,
                       "value": self._dealer.hand.value
                                if not value_hidden else None},
            "players": [self._player_dict(p, p.username == username)
                        for p in self._players],
            "current_username": (self.current_player().username
                                 if self.current_player() else None),
            "deck_remaining": len(self._deck),
        }
```

## 3.3 בדיקות

### 3.3.1 בדיקות שתוכננו מראש

| # | מטרת הבדיקה | מה בוצע בפועל | תוצאות הבדיקה | כיצד נפתרו הבעיות |
|---|---|---|---|---|
| 1 | הרשמה תקינה | שליחת register("alice","secret123") מלקוח | רשומה נוצרה ב‑profiles.db; GUI עבר ישר לשולחן | – |
| 2 | מניעת רישום כפול | רישום "alice" פעמיים ברצף | "username already taken" בפעם השנייה | – |
| 3 | סיסמה שגויה | login עם סיסמה שגויה | "Invalid username or password" (גנרי) | בגרסה ראשונה הודעה חשפה "unknown user"; תוקן לגנרי |
| 4 | מניעת login כפול | login עם אותו שם מחיבור שני | "User already logged in"; חיבור א' לא הופרע | – |
| 5 | משחק מלא לשניים | הימור, Hit/Stand, שלב דילר, בדיקת יתרות | היתרות עודכנו נכון; 3:2 על בלאק‑ג'ק | בהתחלה שולם 1:1 על בלאק‑ג'ק; תוקן לוגיקת התשלום |
| 6 | הימור מעל יתרה | place_bet עם סכום גדול מהקרדיטים | "Insufficient credits"; יתרה לא השתנתה | – |
| 7 | פעולה מחוץ לתור | שליחת action בזמן שזה תורו של שחקן אחר | "Not your turn" | – |
| 8 | האזנה לרשת | tcpdump על פורט 5050 לאחר handshake | רעש בינארי בלבד, תוכן לא קריא | – |
| 9 | הודעה ענקית | שליחת frame בגודל 200 KB | חיבור נסגר אוטומטית, שרת המשיך לעבוד | נוספה בדיקת גודל ב‑recv_frame לפני הקצאת מאגר |
| 10 | persistence אחרי restart | עצירה והפעלה מחדש של השרת | קרדיטים וניצחונות נשמרו ב‑profiles.db | – |
| 11 | ניתוק באמצע סבב | סגירת לקוח בזמן תורו | התור עבר אוטומטית הלאה; הימור נחשב הפסד | נוסף טיפול מלא ב‑on_player_left |
| 12 | ולידציית שם משתמש | ניסיון הרשמה עם תווים מיוחדים (@, !, רווח) | נדחה: "only letters, digits and underscore" | – |

### 3.3.2 בדיקות שנוספו בהמשך

| # | מטרת הבדיקה | מה בוצע בפועל | תוצאות הבדיקה | כיצד נפתרו הבעיות |
|---|---|---|---|---|
| 13 | end-to-end אוטומטי | סקריפט שמחבר שני לקוחות ומשחק סבב מלא | עבר לחלוטין | – |
| 14 | הרשאות קובץ PEM | בדיקת ls -l על server_rsa.pem | -rw------- (0o600) | בהתחלה נשמר עם 644; נוסף os.chmod מיד אחרי השמירה |
| 15 | JSON לא תקין אחרי handshake | שליחת bytes שרירותיים אחרי שלב הצפנה | ProtocolError; החיבור נסגר, שרת המשיך | – |
| 16 | עומס: 10 לקוחות בו‑זמנית | פתיחת 10 חיבורי TCP בו‑זמנית | כולם עברו handshake; CPU <5% | – |
| 17 | אקראיות (secrets vs random) | ניתוח סטטיסטי של 10,000 ערבובים | אין דפוס ניתן לחיזוי | – |
| 18 | ניתוק רשת פתאומי | ניתוק כבל/Wi-Fi באמצע משחק | "Connection lost"; חזרה למסך ההתחברות | – |

---

# 4. מדריך למשתמש – איך מריצים את הפרויקט

## 4.1 עץ קבצים

```
blackjack/
├── README.md
├── תיק_פרויקט.md
├── run_server.py              <- הפעלת שרת (טרמינל)
├── run_server_gui.py          <- הפעלת שרת עם לוח גרפי
├── run_client.py
├── common/
│   ├── card.py
│   ├── deck.py
│   ├── hand.py
│   ├── protocol.py
│   └── crypto.py
├── server/
│   ├── profile_manager.py
│   ├── game.py
│   ├── client_handler.py
│   ├── chat_moderator.py
│   ├── gui.py
│   └── server.py
├── client/
│   ├── network.py
│   └── gui.py
└── data/                      <- נוצר אוטומטית (gitignored)
    ├── profiles.db
    └── server_rsa.pem
```

## 4.2 התקנה

### 4.2.1 דרישות

* Python 3.10 ומעלה (מומלץ 3.11).
* Linux / macOS / Windows עם ממשק גרפי.
* pip זמין.
* רשת מקומית שבה השרת והלקוחות יכולים לתקשר.

### 4.2.2 התקנת חבילות

```bash
pip install cryptography google-genai
```

Tkinter מגיע עם Python. בלינוקס, אם הוא לא מותקן:

```bash
sudo apt install python3-tk
```

סינון הצ'אט עם Gemini הוא **אופציונלי לחלוטין** – אם לא מגדירים מפתח, כל הודעה עוברת. כדי להפעיל:

```bash
export GEMINI_API_KEY="<מפתח שלך>"   # Linux / macOS
set GEMINI_API_KEY=<מפתח שלך>        # Windows
```

### 4.2.3 מה קורה בפעם הראשונה

* אין חשבונות מוגדרים מראש – כל שחקן נרשם בעצמו.
* כל חשבון חדש מקבל **10,000 קרדיטים**.
* גבולות הימור: **50 עד 5,000** קרדיטים לסבב.

### 4.2.4 הגדרת כתובת הרשת

* ברירת מחדל: שרת מאזין על 0.0.0.0:5050; לקוח מתחבר ל‑127.0.0.1:5050.
* לחיבור ממחשב אחר:

```bash
BJ_HOST=192.168.1.10 BJ_PORT=5050 python -m blackjack.run_client
```

### 4.2.5 דרישות חומרה

מינימום: מחשב 1 GHz, 512 MB RAM, פחות מ‑5 MB מקום פנוי. כמעט כל מחשב יעבוד.

## 4.3 הפעלה

### 4.3.1 הפעלת השרת

**א. מצב טרמינל (פשוט):**

```bash
python -m blackjack.run_server
```

**ב. מצב לוח בקרה גרפי (מומלץ):**

```bash
python -m blackjack.run_server_gui
```

לעצירה: Ctrl+C בטרמינל, או כפתור "Stop Server" בלוח הבקרה.

### 4.3.2 הפעלת הלקוח

```bash
python -m blackjack.run_client
```

נפתח חלון עם שדה הכתובת. מזינים את IP השרת, לוחצים Join, ואז נרשמים או מתחברים.

### 4.3.3 תיאור מסכים

* **מסך חיבור לשרת:** כתובת ופורט. ברירת מחדל: 127.0.0.1:5050.
* **מסך התחברות:** שם וסיסמה → Register (בפעם הראשונה) או Login.
* **שולחן – תחילת סבב:** הזן הימור ולחץ Place Bet.
* **שולחן – התורך:** Hit לקלף נוסף, Stand לעצירה, Double להכפלת הימור.
* **שלב דילר:** הקלף הנעלם נחשף; הדילר מושך קלפים אוטומטית לפי הכללים.
* **תוצאה:** באנר זהב = ניצחת; אדום = הפסדת; אפור = פוש (החזר הימור).
* **לוח בקרה לשרת:** רשימת שחקנים + Kick, לוג צבעוני ושלב המשחק הנוכחי.

---

# 5. סיכום אישי / רפלקציה

עבודה על הפרויקט הייתה מסע ארוך. בתחילה היה לי רעיון מעורפל – "אעשה משחק קלפים עם רשת" – אבל מהר מאוד גיליתי שכל מילה מסתירה עולם של החלטות.

**הצלחות:**
* הרגע שהפעלתי שני לקוחות בו‑זמנית והם ראו אותו שולחן – threads הפכו מתיאוריה לכלי עבודה.
* כש‑tcpdump הראה שהתעבורה לא קריאה אחרי handshake – ההצפנה עבדה.
* כשהבנתי לבד למה random לא מתאים לקזינו ועברתי ל‑secrets.

**אתגרים:**
* הבדל בין bytes למחרוזת ב‑Python 3 – שעות על TypeError.
* Race conditions: בגרסה ראשונה לא תמיד ניתן היה להמר כשמישהו אחר שלח Hit באותו רגע. הפתרון: threading.RLock על כל פעולה משנה‑מצב.

**למידה:**
* רוב הלמידה הייתה עצמאית – תיעוד Python, Real Python, Computerphile.
* בכל פעם שנתקעתי כתבתי לעצמי "מה בדיוק אני מנסה להעביר" – הטקסט הזה לבדו פתר חצי מהבאגים.

**הרגלים שאקח הלאה:**
1. להגדיר פרוטוקול תקשורת בכתב לפני שכותבים שורת קוד.
2. להשתמש ב‑logging מסודר ולא ב‑print.
3. להפריד שכבות – common, server, client – כך שכל קובץ קריא לבד.

**במבט לאחור – מה הייתי משנה:**
* הייתי בוחר asyncio במקום threading לניהול ריבוי לקוחות.
* הייתי מוסיף pytest עם בדיקות יחידה מההתחלה.

**שאלות שנשארו פתוחות:**
* האם secrets.randbelow עמיד לחלוטין, או שיש מתקפות על urandom?
* איך עובד OAEP מתחת למכסה המנוע?
* איך קזינואים אמיתיים מוודאים שהדילר שלף קלף מהצמרת?

---

# 6. ביבליוגרפיה

* Python Software Foundation. (2024). *socket — Low-level networking interface*. https://docs.python.org/3/library/socket.html
* Python Software Foundation. (2024). *threading — Thread-based parallelism*. https://docs.python.org/3/library/threading.html
* Python Software Foundation. (2024). *secrets — Generate secure random numbers*. https://docs.python.org/3/library/secrets.html
* Python Software Foundation. (2024). *hashlib — Secure hashes and message digests*. https://docs.python.org/3/library/hashlib.html
* Python Cryptographic Authority. (2024). *Cryptography documentation*. https://cryptography.io/en/latest/
* Bellare, M., & Rogaway, P. (1995). *Optimal asymmetric encryption — How to encrypt with RSA*. EUROCRYPT '94.
* OWASP Foundation. (2023). *Password Storage Cheat Sheet*. https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html
* Tanenbaum, A. S., & Wetherall, D. J. (2011). *Computer Networks* (5th ed.). Pearson.
* Computerphile. (2017). *Public Key Cryptography* [Video]. https://www.youtube.com/watch?v=GSIDS_lvRv4
* Google. (2025). *Google Generative AI Python SDK (google-genai)*. https://ai.google.dev/gemini-api/docs/sdks

---

# 7. נספחים

## נספח א' – הסבר קצר על הטכנולוגיות

### B.1 RSA-2048 + OAEP

RSA היא הצפנה אסימטרית: מפתח ציבורי לכולם, מפתח פרטי רק לשרת. מה שמוצפן עם הציבורי – רק הפרטי יכול לפענח. בפרויקט הזה RSA משמש פעם אחת בלבד לכל חיבור – להעברת מפתח ה‑Fernet. OAEP מוסיף אקראיות לכל הצפנה ומונע מתקפות סטטיסטיות על RSA.

### B.2 Fernet

מפרט הצפנה המבוסס על AES-128-CBC + HMAC-SHA256. כל שינוי בבית אחד בחבילה מוביל לכישלון פענוח. כולל timestamp שמאפשר לגלות הודעות "ישנות" שנשלחו מחדש.

### B.3 PBKDF2

הפעלה חוזרת ומכוונת של HMAC על הסיסמה ו‑salt. 200,000 איטרציות הופכות כל ניחוש בודד לפעולה יקרה חישובית – מה שהופך brute-force לבלתי כדאי.

### B.4 TCP

מקנה זרם בתים אמין בין שני קצוות הרשת. לא כולל מסגור הודעות – את זה אנחנו בונים בעצמנו עם length-prefix.

### B.5 Threads ו‑Locks

Thread הוא נתיב ביצוע מקביל בתוך אותו תהליך. RLock (Reentrant Lock) מבטיח שרק thread אחד יכול לשנות נתון מסוים בכל רגע נתון, ומונע race conditions.

### B.6 Gemini AI

מודל שפה גדול של Google. בפרויקט, Gemini 2.5 Flash מסווג כל הודעת צ'אט כ‑ALLOW או BLOCK תוך שניות. אם ה‑API לא מוגדר או לא זמין, ChatModerator פשוט מעביר את ההודעה הלאה בלי לחסום.

## נספח ג' – קוד מלא (לצירוף בגרסה מודפסת)

יש לצרף את הקבצים בסדר הבא:

1. common/card.py
2. common/deck.py
3. common/hand.py
4. common/protocol.py
5. common/crypto.py
6. server/profile_manager.py
7. server/game.py
8. server/client_handler.py
9. server/server.py
10. client/network.py
11. client/gui.py
12. server/chat_moderator.py
13. server/gui.py
14. run_server.py
15. run_server_gui.py
16. run_client.py
